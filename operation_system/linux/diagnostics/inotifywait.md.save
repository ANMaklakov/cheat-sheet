# inotifywait

***inotifywait - отслеживание изменений в файловой системе***  

- **Установка inotifywait в debian-like дистрибутивах**  
```
apt install inotify-tools
```

- **Отслеживание создания файлов реку**  
```
inotifywait -m -r -e create /
```
