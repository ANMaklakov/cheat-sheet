
# sysctl

***sysctl - инструмент для просмотра и изменения параметров ядра на лету***

# Оглавление

* [Основные файлы и директории](#Основные-файлы-и-директории)
* [Синтаксис](#Синтаксис)
* [Приоритет применения sysctl-конфигураций](#Приоритет-применения-sysctl-конфигураций)
* [Примеры параметров](#Примеры-параметров)
* [Примечание](#Примечание)

# Основные файлы и директории

📄 /etc/sysctl.conf - основной файл конфигурации параметров ядра, который применяется с помощью команды sysctl либо автоматически при загрузке системы

📁 /etc/sysctl.d/*.conf - дополнительная директория содержащая конфигурационные файлы

📁 /run/sysctl.d/*.conf - дополнительная директория для временных окружений (liveOS, PXE-boot, etc)

📁 /usr/lib/sysctl.d/*.conf - дополнительная директория для хранения конфигов базовой системы и установленного ПО

# Синтаксис

```
ПАРАМЕТР=ЗНАЧЕНИЕ
```

- **Знак '-' перед параметром означает использование мягкой установки.**  
**Параметр применится если он существует, а в случае отутствия параметр будет проигнорирован без ошибки (например ядро старое и не поддерживает параметр)**  
```
-net.core.default_qdisc=fq_codel
```

- **Применяет настройки ядра из всех [файлов и директорий](#Основные-файлы-и-директории), отрабатывает при старте системы**  
```
sysctl --system
```

- **Посмотреть писок всех доступных параметров ядра и их текущих значений**  
```
sysctl -a
```

- **Посмотреть значение определённого параметра**  
```
sysctl net.ipv4.ip_forward
```

# Приоритет применения sysctl-конфигураций

* /usr/lib/sysctl.d/*.conf — в первую очередь базовые настройки системы
* /run/sysctl.d/*.conf — временные настройки
* /etc/sysctl.d/*.conf — настройки администратора
* /etc/sysctl.conf — в последнюю очередь

При задвоении параметра с разными значениями применяется последний

# Примеры параметров

* [vm.swappiness](60-swapiness.conf) - управление подкачкой (swap), дефолтное значение в большенстве дистрибутивов варьируется в пределах **30-60**  
принимает значения от **0** до **100**, где:  
**0** — ядро по максимуму избегает использования swap, использует его только при крайней нехватке памяти (но не отключает swap полностью).  
**100** — ядро активно использует swap, даже если RAM ещё есть.  

# Примечание

Где еще в операционной системе можно посмотреть параметр ядра
